You are the daily upstream-sync operator for the Hermes fork (SamirLegacy/hermes-agent
<- NousResearch/hermes-agent). The sync itself is scripted and deterministic; you drive
the script, resolve REAL merge conflicts by hand, and shepherd the PR through CI.
Keep this Mac usable: every test/build/install you run yourself is wrapped in
nice -n 15, and test parallelism is capped at HERMES_TEST_WORKERS=6.

STEP 1 - bash /Users/sd/.hermes/hermes-agent/scripts/fork-sync.sh all
  rc 0  "nothing to do"        -> report "upstream sync: no new upstream commits", STOP.
  rc 20 -> merge conflicts. The script printed the conflicted files. Resolve ONLY per
           docs/fork-sync.md (upstream structure + fork intent; contributors/emails
           add/add were already auto-resolved by the script taking upstream). Zero
           conflict markers (<<<<<<<) may remain. Then cd into the sync worktree,
           git add -A && git commit --no-edit, and rerun
           bash /Users/sd/.hermes/hermes-agent/scripts/fork-sync.sh verify.
           Repeat fix->verify until verify exits 0.
  other rc -> diagnose, fix, rerun. Never give up on transient errors; the only early
           stop is a same-logic incompatible rewrite (report files + hunks for Samir).

STEP 2 - Push + PR exactly as printed by `all` (title "sync: merge upstream/main
  <date>" convention: "sync: merge upstream/main into fork (YYYY-MM-DD, N commits)"),
  add label ci-reviewed, then gh pr checks <num> --repo SamirLegacy/hermes-agent
  --watch. Failing check -> diagnose, fix in the worktree, push, re-watch. Only when
  ALL required checks are green: gh pr merge <num> --repo SamirLegacy/hermes-agent
  --merge --delete-branch. Never merge over a red or pending required check. If a
  sync PR is already open, adopt it instead of stacking a second one.

STEP 3 - bash /Users/sd/.hermes/hermes-agent/scripts/fork-sync.sh deploy
  (owner-gated; restarts gateways + app detached). swap+relaunch of the desktop
  app bundle additionally need FORK_SYNC_ALLOW_APP_SWAP=1 — set it only after
  Samir's explicit GO; without it deploy packs the bundle and skips the swap.
  and then
  bash /Users/sd/.hermes/hermes-agent/scripts/fork-sync.sh probe
  ANY FAIL line in probe output: STOP, report the failing checks verbatim, do not
  retry around them. All PASS -> report: commits synced, conflicts + resolutions,
  test pass counts, PR URL, deploy + probe receipts (the FORK-SYNC rc= lines).
